beautifulsoup4
cryptography
gurux_dlms
pyserial
lxml
# charset-normalizer is used for better performance with BeautifulSoup.
# It replaces cchardet which is not compatible with Python 3.12+.
charset-normalizer

# For data visualization
pandas
matplotlib

# Optional: For interactive cursor tooltips on hover
mplcursors

# For the web-based live monitor (web_monitor.py) - alternative to the
# matplotlib GUI for headless servers / remote viewing. Plotly.js is vendored
# locally in static/plotly.min.js, so no separate Python plotly package is needed.
flask

# Note: tkinter is required for live GUI but cannot be installed via pip
# Install it using your system package manager:
#   Debian/Ubuntu: sudo apt-get install python3-tk
#   Fedora/RHEL:   sudo dnf install python3-tkinter
#   Arch Linux:    sudo pacman -S tk
# For servers without GUI: Use plot_power_data.py instead of live_monitor.py
